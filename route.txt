Paramns = Use Path<T> para extrair parâmetros da rota.
Query-Paramns = Use Query<T> para extrair query strings.
Body = {
    Use Json<T> para JSON.
    Use Form<T> para application/x-www-form-urlencoded.
    Use Bytes ou Body para pegar o corpo cru.
}

